[package]

name = "portaudio_sys"
version = "0.1.0"
authors = ["Mathijs van de Nes <git@mathijs.vd-nes.nl>"]
links = "portaudio"
build = "build.rs"

[lib]
name = "portaudio_sys"
path = "lib.rs"

[build-dependencies]
pkg-config = "*"

[dependencies]
libc = "^0.1.5"
